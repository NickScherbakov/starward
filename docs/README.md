# Starward Landing Page

Современный лендинг для проекта Starward, размещенный на GitHub Pages.

## 🚀 Автоматическая публикация

Лендинг автоматически публикуется на GitHub Pages при каждом push в ветку `main`.

## 📋 Настройка GitHub Pages (первый раз)

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе **Source** выберите:
   - Source: `GitHub Actions`
3. Сохраните изменения

После первого push в `main` ветку, GitHub Actions автоматически развернет сайт.

## 🌐 URL сайта

После развертывания сайт будет доступен по адресу:
```
https://nickscherbakov.github.io/starward/
```

## 📁 Структура

```
docs/
├── index.html     # Основная HTML страница
├── styles.css     # Стили с градиентами и анимациями
└── script.js      # JavaScript для интерактивности
```

## ✨ Возможности лендинга

- ⚡ **Современный дизайн** с градиентами и анимациями
- 📱 **Адаптивная верстка** для всех устройств
- 🎨 **Темная тема** с акцентами на бренд
- 🔄 **Интерактивные элементы**:
  - Табы для примеров установки
  - Копирование кода в один клик
  - Анимации при прокрутке
  - Плавная навигация
- 🎯 **SEO оптимизация** с meta тегами
- ♿ **Доступность** с поддержкой клавиатурной навигации
- 🎪 **Пасхалка** - попробуйте ввести Konami Code!

## 🎨 Секции лендинга

1. **Hero** - Главный экран с призывом к действию
2. **Why Starward** - Проблемы и решения
3. **Features** - Ключевые возможности
4. **Quickstart** - Быстрый старт с табами
5. **Demos** - Реальные примеры использования
6. **Architecture** - Архитектура проекта
7. **Benchmarks** - Метрики производительности
8. **Parity Radar** - Покрытие API облачных провайдеров
9. **Community** - Призыв к участию
10. **CTA** - Финальный призыв к действию

## 🛠 Локальная разработка

Для локального просмотра просто откройте `docs/index.html` в браузере или используйте простой HTTP сервер:

```bash
# Python 3
cd docs
python -m http.server 8000

# Или Node.js
npx http-server docs -p 8000
```

Затем откройте http://localhost:8000 в браузере.

## 📝 Обновление контента

Чтобы обновить лендинг:

1. Отредактируйте файлы в папке `docs/`
2. Закоммитьте изменения:
   ```bash
   git add docs/
   git commit -m "Update landing page"
   git push origin main
   ```
3. GitHub Actions автоматически развернет обновленную версию

## 🎯 Оптимизация

- Использование системных шрифтов для быстрой загрузки
- Минимум внешних зависимостей
- CSS переменные для легкой настройки темы
- Дебаунсинг для оптимизации scroll событий
- Intersection Observer API для ленивой анимации

## 📊 Метрики

После публикации рекомендуется добавить:
- Google Analytics или Plausible для аналитики
- Open Graph изображения для социальных сетей
- Favicon для брендинга

## 🤝 Участие

Если вы хотите улучшить лендинг:

1. Форкните репозиторий
2. Создайте ветку: `git checkout -b feature/landing-improvement`
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - используйте свободно!
